{
  "name": "licensing-tracker",
  "version": "1.0.0",
  "scripts": {
    "fetch:concord": "node scripts/fetch-concord.js",
    "build": "npm run fetch:concord"
  },
  "engines": {
    "node": "18.x"
  }
}
